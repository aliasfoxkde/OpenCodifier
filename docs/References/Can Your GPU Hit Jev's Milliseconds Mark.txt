YouTube Video URL: https://www.youtube.com/watch?v=bcGO7xre46o

71,798 views  Sep 21, 2026
Jev answers in milliseconds, picks from your options instead of writing a paragraph, and you
need a waitlist spot to try it. I added the same way of asking to llama.cpp, so it runs on the
model already sitting on your drive and the card you already own.

A new endpoint on llama-server takes your instructions, your schema and the current state, and
sends back every field with a value and a confidence score. Same server you use for chat, same
model in memory, one extra way to ask it things. On a 2 GB model in 2.4 GB of VRAM that is
17.3 ms per decision when you send them in bulk — and on the recorded demo in this video, 306 ms
against 3,415 ms for writing the same JSON, with all twelve fields identical.

None of this would exist without Harsha, who proved the idea in the open within hours of the
launch instead of keeping it to himself. His repo is below. All I did was carry it over to the
engine the rest of us already run.

So: which models love it, where it falls over, and what you would actually use it for.

⏱️ CHAPTERS
0:00 Intro
0:44 What Jev actually is
3:43 The secret sauce — choose instead of write
9:06 Jev mode in llama.cpp
13:15 Run it yourself — setup, start to finish
17:03 The model hunt: what it loves, what breaks it
20:59 What you'd actually use it for
25:01 What it comes down to

🔗 LINKS
Harsha's model (the original proof, Apache 2.0):
https://huggingface.co/harshatheg/Qwe...
The llama.cpp branch with Jev mode:
https://github.com/thecodacus/llama.c...
The decision playground:
https://github.com/thecodacus/decisio...

🧰 WHAT'S IN THIS VIDEO
· A new `/v1/decision` endpoint on llama-server, and how it is wired
· Constrained choice instead of free text — one answer per field, plus a probability
· Scoring every option in parallel from one cached context
· 17.3 ms per decision in bulk on a 2 GB model, in 2.4 GB of VRAM
· Which models take to it: attention-only models fly, hybrids hit a batching wall
· Where it breaks, and what a wrong pick actually is (it is not a hallucination)
· A playground you can run against your own llama-server

❓ THE QUESTION
If you had a reflex like this wired into your own setup, what is the first job you would hand
it? I have got a list, and I will be sharing that journey with you all on this channel.
AI-generated video summary 
AI can make mistakes, so double-check it. 

Codacus investigates the Jev architecture, exploring how its rapid, type-safe decision-making works compared to standard token-by-token generation. By implementing a similar parallel query method within llama.cpp, this exploration tests whether existing open-source hardware and models can achieve comparable speed and efficiency for structured data tasks.


Transcript:
There's a new AI that answers in milliseconds and its makers say it doesn't hallucinate.  It's called Jev.  It costs 4 cents per million input tokens and you still need a weightless spot to try it.  But get this, Typesafe AI spent 2 years building it in stealth.  Within hours of the launch, one developer opensourced his own version built in 2 hours on an open model you can download today.  So what is Jev actually? Is there anything fundamentally different about it? Can you run the same thing in llama or CPP with the models already out there? And the big one, can the GPU you already own hit Jev's milliseconds mark with the same type safety? Let's find out. So, if you've been anywhere near social media this week, you heard the name Jev. And like me, a lot of you are probably confused.  Is it a classification model? a regression model, some kind of diffusion model.  What's the actual difference? We'll get into that.  But whatever it is underneath, we got to see what it can do.  And that's the part making the internet crazy.  See, the way we use an LLM today, it runs token by token like a writer writing out the whole story, one word at a time.  And when we wanted to call a tool, we ask it for structured JSON, and it writes that out the same way.  every brace, every field name, every value, one token at a time, which takes seconds.  And unless you lock it down, it can miss a field, invent a field name, or wander off and answer something else entirely.  Jeff does it differently.  It answers everything in one pass.  And instead of writing the field names and the values, it only produces the values.  The system collects them, builds the JSON for you and hands you one finished object in a few milliseconds and they mysteriously call it a system one model which is borrowed from Kman's famous book.  Your mind runs two systems.  The slow one that reasons and the reflex that answers before you've thought about it.  Jev is pitched as the fast reflex.  That's the part people latched on to.  it can't make up an answer because the system doesn't let it produce anything that isn't in your schema and within such a short time.  We'll see exactly how that works shortly.  But the possibilities that opens up are huge.  Now, at first I wasn't convinced.  It's another AI with another claim.  So, I went and read what they published about RLCD, the training method they say is behind it. reinforcement learning for calibrated decisions and it's about teaching the model to give you a confidence number that's actually honest.  That's it.  It doesn't explain the millisecond responses and it doesn't explain the no hallucination claim.  And this kind of thing isn't new either.  You can already do something close with a diffusion model.  Google's diffusion Gemma generates a whole block of tokens at once.  You hand it a document, mask the parts you want filled, and it fills only those parts while the rest stays untouched.  That's the entire point of diffusion.  So, if that's what they built, it's really not that impressive, even if the model itself is smart.  It's not something new that gets me excited, and I wouldn't have made this video for it.  What actually got me intrigued is what happened afterwards. So a few hours after that launch, someone on X did the thing I didn't expect. Harsha Gondala opensourced a repo and posted they were building in stealth for 2 years.  I was building in stealth for 2 hours.  What he put up wasn't a new model.  It was an ordinary Quen 2.5, the 1 and a half billion parameter 1.  the same weights anybody can download.  No retraining, no special architecture, just a different way of running it on his Mac.  It came back about six times faster than normal decoding.  And every single answer came out matching the schema.  And I instantly thought, wow, this is genius.  And once you see it, you can't unsee it.  So, let me walk you through what's actually going on because this is the heart of the whole video. Every time a model produces one token, it runs the entire model, all the weights, top to bottom.  What comes out the other end is a score for every single word it could say next.  So in simplified terms, if a model has a couple of hundred,000 words in its dictionary, it scores all of them every time to decide which one comes next. Your inference engine picks one of the highest scoring words, sticks it on the end, and runs the whole thing again for the next one.  That's what makes writing slow.  A 30 field JSON answer is 300 trips through the model and most of those trips are spent producing quote marks, colons, braces and field names you already knew.  Now you might be thinking we already have JSON mode grammar constrainted decoding and yes that fixes the broken JSON because at every step it crosses out the tokens that would make the output invalid but it doesn't magically make the model faster.  The model still writes the field names one token at a time.  valid and just as slow and it can still give you perfectly valid JSON with the wrong answer inside it.  So here's the interesting part.  What if we don't ask it to write the JSON at all? What if we ask the model one thing? What is the value for this one field and it answers with a single token.  Let's say one of the fields is the department a customer's question should go to and it only allows three values, technical, billing, or shipping.  The model can answer any of those three, but it still can't do it in one token because those words are made of several tokens each. So instead of asking for the word, you give it options.  Type A for technical, B for billing, C for shipping.  Now the answer is one token.  A, B, or C.  And to make it even more robust, you only read the scores of those three tokens and ignore everything else.  So even if some other tokens scored higher, the engine never looks at it.  Those are all ignored.  And now you have your answer for that question with a normalized confidence score on each option.  You don't even need the letters, by the way. those options already differ at their very first token.  So you can read the score of the real words instead.  Only when two options start out identically, like very negative and very positive, do you read one more token to separate them? That's what we'll end up doing. Now, what if we do that for all 30 fields in the JSON and ask all of those questions in parallel? It turns into a pre-filled request with a batch of parallel queries.  And if you've run local models before, you know prefill speed is much faster than generation speed.  So we are generating one token per field.  And we are answering every field in a single pass using the same machinery that processes your prompt. Nothing can be hallucinated and every answer lands at once.  And this is the moment that really hit me.  We have been using these models in one single mode this entire time.  There are so many other ways to use the models we already have which means we can add a new inference mode to llama.cpp and give any open model a jev mode.  It's just a new way of querying a model you already downloaded.  And my mind is blown away.  So without wasting any time, I started working on adding jev mode to llama.cpp and I'm going to show you exactly step by step how you can run it on your system.  So let's get into it.  Quick thing before we keep going.  This is what I do on this channel.  Take whatever everyone is losing their mind over this week and find out whether it runs on hardware you can actually buy.  If that's your kind of thing, subscribe.  It genuinely helps.  Right.  Llama.cp. So, the plan was simple.  Take that idea and put it where the rest of us already live.  Llama.cpp. It runs on Nvidia, on AMD, on a plane CPU, on a Mac, and whatever model you downloaded last week, it probably runs that too.  One inference engine, hundreds of models already sitting on your machine.  What I added is a new endpoint on Llama server.  You send it your instructions, your schema, and the current state.  And it sends back every field with a value and a confidence score.  That's JEV mode.  Same server you use for chat, same model loaded in memory, one extra way to ask it things. And the first version worked.  And the answers were subtly wrong.  Here's what I'd missed.  The questions themselves sit up front in the instructions.  Every field what it means and the values it's allowed to take.  But the thing you actually score is the JSON.  The model gets the opening brace, then the field name and the colon, and you read what it would put next.  So where you cut that line matters.  I was breaking it where it looks right to a human after the colon and the space and then scoring the word true.  But that model writing that line on its own never writes it that way.  It writes the colon and then a space and the word together as one piece.  So I was asking it about something it would never have written.  And the confidence scores came back skewed.  The fix is to tokenize the whole line, the field name and the value together and cut where the options actually start to differ.  Then there's the other half of this, and this is where it gets fun.  Look at what you're actually sending on every request.  The instructions, the field list, every allowed value, none of that changes.  The context changes.  The questions don't.  So here's how that gets used.  When a model reads text, it doesn't throw that work away afterwards.  It keeps what it worked out about every token it has read.  And that's the KV cache.  is the thing filling up your VRAMm while you chat. Read a block of text once and the model's memory of it is sitting right there.  So we read the instructions and the schema once and keep that cache. Every request after that starts from a model that has already read them.  Then the request goes on top read a single time and now that cache holds everything all the questions need.  And this is the part that makes it parallel.  The questions don't each get their own copy of that memory.  They all point at the same one.  Each question adds only its own few tokens on the end, the field name and the colon, and reads its answer from there.  The expensive part gets paid once, and each extra question costs you a handful of tokens.  And once that's true, there's no reason to send your requests one at a time.  You can hand over eight of them in a single request. The instructions are already sitting there.  So each request just continues from them.  All of them go through the model together and every question on every request comes back in the same pass.  Those eight requests in one call landed about 12 times faster than asking the same model to write the eight answers out as JSON.  And again sending those same eight through JV mode one after another.  Batching them still roughly doubles what the GPU gets through and about quadruples it once you count the round trips you're no longer making.  So it runs it runs on any model llama.cpp can load on the card you already own.  But there's a difference between faster and fast enough.  JV is claiming answers in milliseconds.  So, could my own GPU actually hit that mark? So, let me show you how to run this yourself.  It's about 10 minutes of setup and everything I'm using is open.  First, you clone my fork and switch to the branch called parallel decision.  That's where Jev mode lives.  It isn't on main. So, if you forget this step, you'll build a perfectly ordinary llama.cpp. Everything after that is the same drill you already know.  Then you build it for whatever you've got.  On an Nvidia card, you turn on CUDA.  On a Mac, it's metal, which is already on by default, so you just build.  On AMD, there's a rocom switch.  And if you've got none of that, it still builds and runs on the CPU, just slower.  The build takes a few minutes.  Next, a model.  Anything llama.cpp can load will work here.  So, grab a gguf you already like.  I'll use Gemma 412B for this because it's the one I trust most for judgment calls and it fits on a 12 GB card.  Now, Llama server. You can point it at a single model file, but I'd rather show you the presets file because it's the thing you'll actually keep.  It's a plain text file.  Each block is a model with the settings that model wants.  how many layers go on the GPU, the context size, and the one setting this whole video is about, which is how much room to keep for those parallel questions.  Give it a dozen, and a dozen questions can be in the air at once. Then you start the server pointing at that file.  Same command you already use plus the presets.  And when it comes up, it's serving chat like always, and it's serving decisions on the new endpoint. That's the engine running.  But before you wire it into anything real, you want to see what it does.  So, I built a playground.  You clone it, install, and run it.  It's a web app.  It runs in your browser, and there's no backend of its own.  It talks straight to your Llama server, which means your data goes nowhere.  It's got one setting, the address of your server.  You paste that in and if it can see it, the model list fills in by itself.  Then you pick one of the presets.  Let's take the support ticket one.  On the left is your schema. Every field with its allowed values and underneath it the context which is the customer's message.  And you fire the same question two ways.  One goes to the decision endpoint.  The other goes to the same model as a normal chat completion, writing the JSON out by hand.  Both timers run live and it's not really a contest.  The decision lands while the other one is still typing its second field.  300 milliseconds against 3 and 1/2 seconds.  Same 12 answers, both of them.  And every field comes back with a confidence number, which is the part I care about more than the speed.  Some of them land at 100%.  And some sit a lot lower, which is the model telling you where it was guessing.  So that's the whole setup.  Your model, your card, your data, and decisions coming back instantly.  But this was one model.  When I started putting other models through it, some of them flatly refused to get fast.  A 4 billion parameter model that wasn't any quicker than a 12.  a 35 billion that couldn't get under a second no matter what I did and the fast ones get things wrong.  So the real question is which model you should run and what you can trust it with. So with the thing working the obvious next move was to throw models at it. Nothing changed except the model and the first result made no sense.  A 4 billion parameter model came back no faster than a 12 billion one.  On paper, that small one reads a prompt more than twice as fast, which is the exact thing we are leaning on here.  So, I went looking and the answer is what kind of model it is. That one's a hybrid.  Some of its layers are normal attention.  The rest are recurrent, which means they carry a running state forward instead of looking back over everything.  And a recurrent layer needs every question in the batch to be the same length.  Ours aren't.  So, llama.cpp does the only thing it can. Instead of one fused batch with every question riding along, it splits the work into separate runs.  Each run is still parallel.  It's just not the single clean pass we were after.  Our 14 questions became something like eight parallel trips.  Reading the context was still fast.  What got taken away was the fused kernel path, the optimized one. And this isn't a reason to write off hybrid models.  That same hybrid answered six fields in about 100 milliseconds. The same model writing that JSON token by token took nearly 900 8 and a half times faster on the slow path.  The engine just doesn't have that optimization for hybrids yet and engines get optimized.  Attention only models get the whole benefit today.  Then I tried the big one 35 billion parameters.  The mixture of experts model I use for real work and it would not get under a second.  No setting moved it.  And the reason is something I'd had wrong.  Most of that model's experts live in system RAM because they don't fit on the card. Any batch bigger than about 30 tokens makes llama.cpp copy those weights across to the GPU.  12 GB over the bus. And it's the same 12 GB whether that badge is a,000 tokens long or 75 which finally explained a number I'd quoted before.  That model reads a,000 tokens a second at a,000 tokens per batch.  Feed it 75 at a time and you get a sixth of that.  The big benchmark number was never describing this kind of work.  So, the method holds up fine.  What moves the result is which model you hand it to.  I ran 18 game situations through every model I had.  Aiming and shooting calls where I already know the right answer. The 35 billion got all 18.  The 12 billion got 15 in about a fifth of the time.  The 2 billion one got three.  Give that same 2 billion model a support ticket, though, and it lands on the same category and the same priority as the big ones in a few milliseconds.  It's a smaller brain.  Geometry is hard for it. Routing an email isn't.  And on the hard task, when it was wrong, it never made anything up.  Every answer was a legal value from my schema.  and the JSON came back instantly.  It was just the wrong call.  That's not hallucination.  That's a wrong judgment.  And no amount of type safety protects you from it.  So the thing to check before you ship isn't your format.  That part is guaranteed. It's whether this model knows your task. There's a second half to that too.  Those questions can't see each other. Remember? So I gave it a 30 field order where some of the rules depend on other answers.  Counting how many units are still standing.  that sort of thing.  The plain model writing JSON beat it on most of the ones they disagreed about.  It could look back at what it had already written.  Jev mode can't.  And that's not something I broke.  Types safe about their own model.  Use this for questions that don't depend on each other.  That's the deal for all of us.  So instant, always valid, and a confidence score on every field.  What do you actually do with a thing like that? Okay, so we have a new way of running an LLM.  But what's the use of it beyond all the crazy posts we are seeing on X right now? Well, this is the part that really brings out the experimentter in me.  If you've read Daniel Carneaman's thinking fast and slow, it describes two kinds of thinking.  System one is the fast one.  It hands you an answer immediately and that's what we call intuition or muscle memory.  When it's something physical, then there's system two, the slow one, which actually works the problem and gives you a stronger, more accurate answer.  And system two is expensive.  So, evolution decided to use it sparingly to save energy and to keep it for the problems system one can't solve.  Let me put that in a scenario.  You're walking down the street with your phone in your hand.  You're reading a message, working out what to reply.  That's system two. And it can only do one thing at a time. Meanwhile, your legs are handling the walking.  You're not thinking about the walking at all.  Something else is running that along with your balance, the people coming the other way, the curb you step off without looking. That's system one.  It runs a dozen little routines at once, and never asks you for attention until it hits something it can't handle.  A car turns into your path, your eyes can see it. But system two has been logged on that little patch of screen in your hand this whole time.  So it doesn't notice.  System one noticed.  It's been picking up all of those small details the whole way.  And it can't solve this one.  So it does the only thing it can do.  It interrupts you. And then system two takes over.  You stop.  You look.  You work out what's happening.  You move.  Then the walking goes back to running on its own.  That's the architecture I've been thinking about since I first heard Typesafe call this thing a system 1 model and then saw it running for myself.  And I'm fairly sure that's exactly why they picked that name for it.  Okay.  So what are the practical ways we can put this to work? Take a support inbox.  Thousands of emails.  Every one of them needs a category, a priority, a yes or no on whether a human has to see it.  That's system one work.  Jev mode handles the whole pile in bulk, milliseconds each, and every answer comes back with a number attached saying how sure it was. And then anything the model was confident about goes straight through. Anything it wasn't, you hand to a bigger model that can actually think about it slowly, one at a time.  That's your system two, which is fine because that's a handful of emails now, not thousands. Same shape works anywhere the job is a reflex.  Game agents picking their next move several times a second, which is the demo I keep replaying.  Home automation where every light and every blind is a field in one schema. Moderation, routing, anywhere you're asking small questions about something that just arrived.  So which model? If the job is sorting and routing, a 2 billion parameter model on an 8 GB card will do it and it'll do it faster than you can refresh the page.  If the decision is more complex, the kind where being wrong costs you something, that's where the 12 billion Gemma 4 is a good pick.  And for the ones it isn't sure about, that's when you switch mode and do the slow thinker version of the model. So, back to where we started.  Jev picks from your options instead of writing you a paragraph and it comes back in milliseconds.  What's underneath it? Nobody outside that company can tell you.  But the founder has said he co-invented the reinforcement learning behind chat GPT.  The work that turned a next token predictor into a thing that answers your questions.  And then he said chat was a detour and he wanted to do something else entirely.  You can connect the dots.  But what matters now is that an ordinary open model asked a different way does the same job.  It runs in llama.cpp on the files already sitting on your drive on the card you already own and the answers come back in milliseconds. The hyped version is behind an API with a weight list and weights nobody has seen.  The open version runs on your card on your data with a model you picked and you can read every line of how it works. And none of this would have happened without Harsha who spent hours proving the idea in the open when he could have kept it to himself.  All I did was carry it over to the engine the rest of us already run.  His repo is in the description along with the branch and the playground.  So, here's what I want to know from you.  If you had a reflex like this wired into your own setup, what's the first job you'd hand it? because I've got a list and I'll be sharing my journey with you all on this