{
  "name": "opencodifier_decide",
  "description": "Answer every decision question exactly once, using the provided state.",
  "input_schema": {
    "type": "object",
    "title": "opencodifier_decisions",
    "description": "One field per decision question, answered in a single response.",
    "properties": {
      "engine": {
        "type": "string",
        "title": "Which storage engine holds the artifacts?",
        "description": "opencodifier:descriptions=[\"embedded, zero ops\",\"operational overhead, remote access\"]",
        "enum": [
          "sqlite",
          "postgres"
        ]
      },
      "model": {
        "type": "string",
        "title": "Which model should serve this request?",
        "description": "opencodifier:descriptions=[\"General coding and reasoning\",\"Complex reasoning\"]",
        "enum": [
          "local-qwen",
          "local-glm"
        ]
      }
    },
    "required": [
      "engine",
      "model"
    ],
    "additionalProperties": false
  }
}
